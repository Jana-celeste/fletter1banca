name: flutter_bancadejornal_1
description: "A new Flutter project."
publish_to: 'none' # Para projetos privados

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.1.10  # <-- Adicionada a dependência

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
